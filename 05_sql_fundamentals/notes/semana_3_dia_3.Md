🔹 Conteúdo do dia

SQL: Tipos de JOIN

INNER JOIN

LEFT JOIN

RIGHT JOIN

FULL JOIN

15 exercícios de JOIN

Todos explicados com comentários

Foco em leitura, entendimento e aplicação real

1️⃣ Resumo do conteúdo
🔗 O que são JOINs?

JOINs servem para relacionar dados entre tabelas diferentes usando uma chave em comum (geralmente id / foreign key).

Tipos principais:

INNER JOIN

Retorna apenas registros que existem nas duas tabelas

LEFT JOIN

Retorna tudo da tabela da esquerda, mesmo sem correspondência

RIGHT JOIN

Retorna tudo da tabela da direita (pouco usado)

FULL JOIN

Retorna tudo de ambas as tabelas (com ou sem correspondência)

Onde isso é usado no mundo real?

Relacionar:

pedidos ↔ clientes

eventos ↔ usuários

pagamentos ↔ transações

Base de ETL, analytics e pipelines de dados