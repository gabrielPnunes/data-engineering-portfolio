1️⃣ Resumo do conteúdo
📊 Funções de agregação

Funções de agregação resumem dados:

COUNT() → quantidade de registros

SUM() → soma

AVG() → média

MIN() / MAX() → menor / maior valor

📦 GROUP BY

Usado para agrupar linhas antes de aplicar agregações.

Regra de ouro:
👉 tudo que está no SELECT e não é agregação, precisa estar no GROUP BY.

🚦 HAVING

Filtra depois da agregação

WHERE filtra linhas, HAVING filtra grupos